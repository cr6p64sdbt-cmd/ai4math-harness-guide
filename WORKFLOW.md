# Harness 学习与介绍工作流

更新：2026-09-20。目标依据是[项目任务说明](PROJECT-BRIEF.md)。工作主线是理解AI怎样被组织起来研究数学，默认不读其生成数学论文的证明正文，不做数学独立审稿。

## 1. 恢复与限定本轮交付

读INDEX、STATUS、PROJECT-BRIEF，按需读目标系统档案。优先复用已取得的来源和固定版本。明确本轮是候选筛选、框架总览、全框架教程还是特定模块；不因状态列有数学缺口而自动进入证明任务。

当前优先完成一个系统的纵向介绍，再横向扩充。候选数量、领域相关性和名气不替代方法价值。

## 2. 选择与来源

选择有可信成果背景且工作流材料足以解释的框架。官方README、固定代码、配置、prompts和运行样例优先；系统论文用于设计动机与效果报告。数学成果只作简要背景，标明作者报告/已有外部评审/形式工件等事实，不把本项目未重验当作来源质量差。

保存URL、访问日、commit或版本、确实读过的文件/函数。当前框架与旧案例分开，未知历史运行不可由最新代码补写。源码公开不等于原运行可复现。

数学成果只做简要质量筛选：确认原始来源、成果类别与AI参与；检查是否存在明显夸大或可信来源已指出的重大问题。足以筛选就停止，不再代入方程、重算候选或读完整证明。对未能快速判断的疑点写限制，不把缺独立验收当作失败。

## 3. 有界委派，控制总token

遵循全局agent路由，默认按接口拆两个互不重叠任务，例如：

- 易验收的README/配置/prompts/CLI入口定位：轻量research_helper；输出紧凑表和固定来源位置。
- 控制流、状态、异常与恢复：research_worker处理限定源文件；复杂设计判断保留给主控。

角色是按本轮需要选择，不固定每次全部启动。委派提供精确目标、输入版本、允许写入路径和停止条件；默认不再委派。新任务优先不给完整对话历史，只传必要输入；共享文件一个写入者。主控不重做全部定位，仅抽查承重分支、接口和有冲突的地方。源代码不大段贴回主控；重复读取或重新获取须有明确用途。

更强模型用于确有难度的架构/接口问题，不为介绍harness默认启动数学证明代理。少用token不能以漏讲核心控制流为代价；没有真实计量时不声称节省某个百分比。

## 4. 从输入到输出贯通写作

按如下顺序组织中文正文：用途与边界 → 一张总图 → 模块地图 → 输入与配置 → 一轮完整任务 → prompt/上下文 → 状态与目录 → 反馈和换路 → 停止/异常/恢复 → 输出与人的介入 → 最小使用路径 → 复用判断。

解释每个机制解决什么困难、依赖什么条件、失败会怎样；关键实现提供固定文件/函数或行位置。未有日志时，用明确标注的教学走读演示控制流，不伪造真实成果运行。

成果背景够读者理解用途即可。不开数学证明审查支线、不补写定理证明、不默认下载数学成果PDF、运行证书或Lean。用户另行明确要求后才切换任务范围。

## 5. 全框架完成度

逐项查输入、角色、调度、模型接口、工具、上下文、记忆、状态、输出解析、错误处理、反馈、路线调整、预算、停止、恢复、人类操作与来源。没有的能力写“该版本未实现/未公开”，不是用其他系统功能补齐。当前实现若没有子问题并行执行、知识图谱或自动kernel，就明确解释其真实组织方式。

说明是只读源码介绍还是实际试运行；安装/运行不属于当前授权。示例命令必须来自已读版本并带前提，不写成已经在本机验证。

## 6. 质量与接受

来源薄弱、成果仅宣传、框架细节不足者不为凑数进入核心教程。已有方法进展/基准工作可作为对照，按实质方法价值判断；不要求全部是新定理，不以“我没证明它”拒收。正式发表不能证明harness可靠，多agent也不能证明优于单agent。

数学状态字段沿用既有schema，作为防止夸大的边界，不是本轮必须提升的任务。LLM verifier只能介绍其检查目标、调用与判定流程，不把内部PASS写成数学正确。

## 7. 文件与统计验收

核对关键调用链、有效配置、失败/停止分支、固定版本和来源链接；检查Markdown本地链接、元数据和导航。仅有事件变化才重新生成STATS，否则用既有`--check`，不改统计逻辑。系统教程不强制新增事件或数学案例。

更新替换式STATUS与INDEX。历史数学审计保留可追溯，但不占据主要阅读入口，也不继续以补数学证明缺口作为默认下一动作。

默认中文、公开来源、不安装外部系统、不写其他项目、不自动化。对外发布须有用户明确授权，并在发布前检查文件清单、来源许可和凭据；本项目的首次公开发布已获授权。
