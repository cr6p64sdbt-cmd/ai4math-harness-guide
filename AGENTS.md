# `search ai for math` 项目规则

本项目是按需更新的 AI-for-Math harness 检索与研究方法学习仓库（证据用于忠实介绍，不以数学审稿为主）。流程文件、模板、档案、导读及交流默认使用中文；仅必要的数学术语、模型名、命令、代码标识和固定状态值保留英文。

## 任务恢复顺序

每次开始工作时按以下顺序恢复：

1. 阅读 `INDEX.md`；
2. 阅读 `STATUS.md`；
3. 只读取本次任务需要的 `WORKFLOW.md`、事件卡、系统档案或案例 dossier；
4. 不默认加载整个项目，也不凭对话记忆声称当前状态。

## 当前任务优先级（用户最新澄清）

权威任务依据见[PROJECT-BRIEF.md](PROJECT-BRIEF.md)，含本对话用户提示摘录与执行解释。先完成至少一个harness全框架中文教程，重点输入、角色、工具、上下文、状态、调度、反馈、换路、停止、恢复及复用入口。

数学只做简要筛选以丢弃明显来源薄弱、成果与宣传严重不符的harness；疑点标注后停止，不要求逐篇裁定。默认不证明或详细核对AI生成的数学结论，不读成果文章证明正文，不运行数学证书/Lean。只简述成果背景及来源；证据纪律仍用于避免夸大，但不得据此自行扩大为数学审稿。已知数学疑点保留为来源限制，不作为所有harness必须通过独立证明验收的门槛。

沿用全局agent分发：对独立、可检查的来源定位或源码模块主动委派，轻量提取用research_helper、有界实现分析用research_worker；主控整合与抽查关键接口。默认最小上下文、无下级委派、单文件单写入者，避免重复读整套来源。具体任务与模型按全局规则选择，不另建固定角色矩阵，也不声称未经计量的token节省。

## 核心交付

- 按需专题更新，不设固定周产量。2026-08-24 至 2026-09-20 的旧试点按实际文件归档，不补造未执行的周报。
- 核心阅读路径为任务说明、完整框架教程、候选目录、系统档案、方法比较；数学案例仅作背景；候选目录不计入事件统计。
- 系统机制的可学习程度与数学成果的证据强度分别评价，不以名气、模型或作者声明预设成功。
- 重点覆盖数学推理、自动证明、形式化、猜想或反例发现，以及 AI 主导的大型数学工作。
- AI 仅用于语言润色、而数学内容主要由人类完成的案例通常不进入核心案例；基准与竞赛可作对照，须明确并非新数学发现。
- 本项目负责观察、介绍、统计和审计，不负责证明目标论文中的数学结论，也不自动向其他项目推广机制。

## 数学主张与证据规则

- 严格区分 `PROVED`、`CONDITIONAL`、`CONJECTURE`、`DISPROVED`、`OPEN`、`FAILED-ROUTE` 和 `UNASSESSED`。
- 新闻、博客、社交媒体、搜索摘要、OCR 和 AI 总结只能作为 locator。
- 论文标题、摘要或作者自述不能单独把数学状态提升为 `PROVED` 或 `DISPROVED`。
- 对外声明、项目评估和来源读取深度是不同字段，不得混写。
- 未完成声明范围内的检索时，只能写 `NOT-FOUND-IN-CHECKED-SET`，不得声称“没有相关工作”或“已经穷尽”。
- LLM verifier 是对抗性审稿人，不是 formal kernel，也不是独立数学证明。

## 系统档案与案例档案的分工

- `systems/` 记录一个系统的稳定知识：版本、架构、模型角色、工具、记忆、验证、安装与运行接口、安全边界、成本和复现条件。
- `dossiers/` 记录某个具体成果怎样使用这些系统：输入、时间线、模型配置、运行规模、人的介入、产生的数学工件和独立验证。
- 同名系统在不同论文中可能使用不同版本。必须绑定日期、版本或 commit；不得把新版配置倒填到旧案例。
- “机制”首先是可核查的工作流和贡献记录，不等于可迁移方法，不得自动修改 `research 1` 或任何其他项目。

- 档案须解释数学对象、问题和具体进展，以来源支持的实际步骤重建过程；教学例子与真实运行分开，未公开步骤不得用架构推测补齐。
- 成果概述给原始出处并标作者报告；仅另行数学审读时才追定理/证明定位。代码机制绑定commit或明确检索日期。源码可读不等于历史运行可重现。

## 来源与PDF材料规则

- 优先使用论文最新版、官方代码仓库、正式出版页面、作者附录和可检查的证明工件。
- 发现、引文网络、全文取得和证据接受是不同操作，分别记录。
- 只有需要反复精读的重大来源才保存 PDF；统一放在 `references/papers/`，记录来源和 SHA-256，PDF 默认不进入 Git。
- 仅在用户另行委托定理/证明级复用时按全局文献规则决定摘录；harness介绍不自动触发数学转译。
- 遇到 SSO、MFA 或 CAPTCHA 时停止该来源的操作并保留会话，不请求或记录密码、Cookie、OTP、验证码或 session token。

## 安全与项目边界

- 不安装、运行或接入 Danus、QED 等高权限外部系统，除非用户另行明确授权了隔离试验。
- 若将来试运行外部 agent harness，应使用一次性、隔离的环境，先审计权限、费用、密钥范围和停止条件。
- 不写入其他研究项目；跨项目比较必须另开明确任务。
- 默认公开来源、手动按需更新，不创建自动化任务；本批不审计 `research 1`。

## Git操作边界

- 本仓库已获用户明确授权公开发布到其 GitHub。发布前检查将公开的文件、来源许可与凭据；后续新增对外发布或改变权限仍按用户当次要求执行。
- 跟踪 Markdown、脚本、模板和来源元数据；不跟踪凭据、运行时目录和 PDF。
- `STATUS.md` 是替换式当前状态，不作为逐轮日志。

## 扩充与学科相关性（2026-09-20）

深读不再限定四个。按数学价值、证据成熟度、方法透明度和用户相关性分别分类；先补高价值研究，再补局限或基准对照。默认中文，来源与固定版本必须可追踪。★平面ODE、◆其他有限维动力系统、◇相邻PDE分开标注；“可迁移”不等于已有该方向成果。少量来源剖面不计作完整harness深读，未核候选不进入成果事件统计。

## 专题收录质量门槛（2026-09-20补充）

方向相关不能降低质量门槛。收录须有可追踪原始来源、具体且有实质内容的结果、可解释的研究方法；来源不可靠或结果不足以支撑方法学习价值者不纳入。不能用简单教学例子代替整篇成果评价，也不能把尚未查清误判为工作质量差。方法进展、研究级新定理和基础对照分别定位；低水平对照不为数量而收录。正式发表不是数学正确性保证，数值发现也不因非证明而自动判为无价值。
